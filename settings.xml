<settings>
    <servers>
        <server>
            <id>ossrh</id>
            <username>${env.OSSRH_NEXUS_USERNAME}</username>
            <password>${env.OSSRH_NEXUS_PASSWORD}</password>
        </server>
        <server>
            <id>gpg.passphrase</id>
            <passphrase>${env.GPG_PASSPHRASE}</passphrase>
        </server>
    </servers>
</settings>